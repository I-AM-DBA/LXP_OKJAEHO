<?xml version="1.0" encoding="UTF-8" ?>
<properties>
  <query key="course.save">
    INSERT INTO courses (author_id, title, description, status)
    VALUES (?, ?, ?, ?)
  </query>
  <query key="course.select">
    SELECT * FROM courses WHERE author_id = ?
  </query>
  <query key="course.update">
    UPDATE courses SET title = ? WHERE author_id = ?
  </query>
  <query key="course.delete">
    DELETE FROM courses WHERE author_id = ?;
  </query>
  <query key="curri.allSelect">
    SELECT * FROM curri
  </query>

</properties>