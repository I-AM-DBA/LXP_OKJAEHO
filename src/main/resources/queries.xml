<?xml version="1.0" encoding="UTF-8" ?>
<properties>
  <query key="course.allSelect">
    select c.course_name,
    s.section_name,
    ct.content_name,
    ct.content_data
    from courses c
    left join sections s on c.course_id = s.course_id
    left join contents ct on s.course_id = ct.course_id;
  </query>
</properties>